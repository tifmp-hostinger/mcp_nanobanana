{
  "name": "fmp-nano-banana-mcp-remote",
  "version": "2.0.0",
  "description": "Servidor MCP remoto (HTTP) da FMP para gerar imagens de fundo (bg) do LXP com o Gemini 2.5 Flash Image (nano banana). Brand-safe por padrao; autenticado por token.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "express": "^4.21.2",
    "sharp": "^0.33.5",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
